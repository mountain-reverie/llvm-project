//===-- SHInstPrinter.h - Convert SH MCInst to assembly syntax ----*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//

#ifndef LLVM_LIB_TARGET_SH_MCTARGETDESC_SHINSTPRINTER_H
#define LLVM_LIB_TARGET_SH_MCTARGETDESC_SHINSTPRINTER_H

#include "llvm/MC/MCInstPrinter.h"

namespace llvm {

class SHInstPrinter : public MCInstPrinter {
public:
  SHInstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
                const MCRegisterInfo &MRI)
      : MCInstPrinter(MAI, MII, MRI) {}

  void printRegName(raw_ostream &OS, MCRegister Reg) override;
  void printInst(const MCInst *MI, uint64_t Address, StringRef Annot,
                 const MCSubtargetInfo &STI, raw_ostream &O) override;

  // Autogenerated by tblgen (no STI in these signatures for this target).
  std::pair<const char *, uint64_t> getMnemonic(const MCInst &MI) const override;
  void printInstruction(const MCInst *MI, uint64_t Address, raw_ostream &O);
  bool printAliasInstr(const MCInst *MI, uint64_t Address, raw_ostream &O);
  static const char *getRegisterName(MCRegister Reg);

  // Called from generated printInstruction (no STI)
  void printOperand(const MCInst *MI, int opNum, raw_ostream &OS);
  // Called from code that has STI
  void printOperand(const MCInst *MI, int opNum, const MCSubtargetInfo &STI,
                    raw_ostream &OS);
};

} // end namespace llvm

#endif // LLVM_LIB_TARGET_SH_MCTARGETDESC_SHINSTPRINTER_H
